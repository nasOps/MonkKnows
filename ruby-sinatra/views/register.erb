<h2>Sign Up</h2>

<div id="error-message" class="error" style="display:none">
  <strong>Error:</strong> <span id="error-text"></span>
</div>

<form id="register-form">
  <dl>
    <dt><label for="reg-username">Username:</label></dt>
    <dd><input type="text" id="reg-username" name="username" size="30"></dd>
    <dt><label for="reg-email">E-Mail:</label></dt>
    <dd><input type="email" id="reg-email" name="email" size="30"></dd>
    <dt><label for="reg-password">Password:</label></dt>
    <dd><input type="password" id="reg-password" name="password" size="30"></dd>
    <dt><label for="reg-password2">Password <small>(repeat)</small>:</label></dt>
    <dd><input type="password" id="reg-password2" name="password2" size="30"></dd>
  </dl>
  <div class="actions">
    <input type="submit" value="Sign Up">
  </div>
</form>

<script>
  document.getElementById('register-form').addEventListener('submit', function(e) {
    e.preventDefault();

    const formData = new FormData(this);

    fetch('/api/register', {
      method: 'POST',
      body: new URLSearchParams(formData)
    })
    .then(response => response.json())
    .then(data => {
      if (data.statusCode === 200) {
        window.location.href = '/';
      } else {
        const msg = data.detail ? data.detail[0].msg : data.message;
        document.getElementById('error-text').textContent = msg;
        document.getElementById('error-message').style.display = 'block';
      }
    });
  });
</script>